---
name: commit
description: Project skill for creating git commits in metodo following project conventions. Use when the user asks for /commit, to commit changes, or to file back work into history.
---

# commit

Use the project skill as the canonical source of instructions:

`../../../.claude/skills/commit/SKILL.md`

When this skill triggers, read that file and follow it. This wrapper exists only so Codex can discover the project-level skill without duplicating the workflow.
